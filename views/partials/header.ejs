<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/jobs">
      JobsTracker</a>
    </div>
    <ul class="nav navbar-nav navbar-left">
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li><a class="blue" href="/">home</a></li>
      <% if (currentUser) { %>
        <li><a class="blue" href="/jobs">jobs</a></li>
      <% } %>
      <% if (currentUser) { %>
        <li><a class="blue" href="/users/show"><%= currentUser.local.email %></a></li>
        <li><a class="blue" href="/logout">logout</a></li>
      <% } else { %>
        <li><a class="blue" href="/login">Login</a></li>
      <% } %>
    </ul>
  </div>
</nav>
<div class="flash">
  <% if (typeof message !== 'undefined') { %>
    <% if (typeof message.error !== 'undefined' && message.error.length > 0) { %>
      <div class="bg-danger"><%= message.error %></div>
    <% } %>
    <% if (typeof message.info !== 'undefined' && message.info.length > 0) { %>
      <div class="bg-info"><%= message.info %></div>
    <% } %>
    <% if (typeof message.success !== 'undefined' && message.success.length > 0) { %>
      <div class="bg-success"><%= message.success %></div>
    <% } %>
  <% } %>
</div>
